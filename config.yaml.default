app:
  listen: :8080
  auth: changeme # api 接口认证的key  http header名字 x-api-auth
  data_dir: /tmp/httpbash
  display_url: http://127.0.0.1:9191 #服务本身访问的url地址


# 为了安全只有在 command.whitelist 中的命令才能执行
command:
   whitelist:
   - ping
   - ls

